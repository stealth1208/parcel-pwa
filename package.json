{
  "devDependencies": {
    "parcel-plugin-static-files-copy": "^2.1.2"
  },
  "name": "pwa-parcel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html -p 3000",
    "build": "yarn clean && parcel build --no-content-hash index.html *.js *.png *.ico *.css *.json",
    "clean": "rimraf './dist/'"
  },
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
